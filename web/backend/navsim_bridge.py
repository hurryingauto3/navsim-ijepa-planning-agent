from typing import Any, Dict


def run_agent_on_scene(model_name: str, scene_token: str) -> Dict[str, Any]:
    """Placeholder for a NavSim integration call.

    Replace this stub with logic that runs an agent within NavSim and returns
    a payload compatible with the cached replay schema (timestamps_ms,
    ego_traj, agents, metrics_per_tick, aggregate, etc.).
    """
    raise NotImplementedError
