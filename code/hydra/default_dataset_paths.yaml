# navsim/navsim/planning/script/config/common/default_dataset_paths.yaml

# navsim_log_path: ${oc.env:OPENSCENE_DATA_ROOT}/navsim_logs/${train_test_split.data_split} # path to log annotations
# original_sensor_path: ${oc.env:OPENSCENE_DATA_ROOT}/sensor_blobs/${train_test_split.data_split} # path to log annotations
# synthetic_sensor_path: ${oc.env:OPENSCENE_DATA_ROOT}/warmup_two_stage/sensor_blobs # path to sensor blobs
# synthetic_scenes_path: ${oc.env:OPENSCENE_DATA_ROOT}/warmup_two_stage/synthetic_scene_pickles # path to synthetic scenes
# metric_cache_path: ${oc.env:NAVSIM_EXP_ROOT}/metric_cache # path to metric cache


# --- MODIFIED ---
navsim_log_path: ${oc.env:OPENSCENE_DATA_ROOT}/${train_test_split.data_split}_navsim_logs/${train_test_split.data_split} # path to log annotations
original_sensor_path: ${oc.env:OPENSCENE_DATA_ROOT}/${train_test_split.data_split}_sensor_blobs/${train_test_split.data_split} # path to sensor blobs
# --- END MODIFIED ---

synthetic_sensor_path: ${oc.env:OPENSCENE_DATA_ROOT}/warmup_two_stage/sensor_blobs # path to sensor blobs (Keep as is unless you moved these)
synthetic_scenes_path: ${oc.env:OPENSCENE_DATA_ROOT}/warmup_two_stage/synthetic_scene_pickles # path to synthetic scenes (Keep as is unless you moved these)

metric_cache_path: ${oc.env:NAVSIM_EXP_ROOT}/metric_cache # path to metric cache (Keep as is, or change if needed)
